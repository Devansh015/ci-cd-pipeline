{
  "compilerOptions": {
    "checkJs": true, // allows for type checking for JS files
    "module": "commonjs", // Jest uses CommonJS
    "baseUrl": ".", // Base URL for resolving modules
    "paths": {
      "jest": ["node_modules/jest"]
    }
  },
  "include": ["**/*.js"]
}
